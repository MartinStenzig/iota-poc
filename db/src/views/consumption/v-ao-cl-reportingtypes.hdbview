VIEW V_AO_CL_REPORTINGTYPES COMMENT 'Consumption Layer View for ReportingTypes' AS
SELECT DISTINCT
    MAT.SOURCE_SYSTEM || '|' || MAT.MANDT || '|' || MAT.ILART AS ID,
    MAT.MANDT Client,
    MAT.ILART Code,
    MAT.ILART sapILART
FROM
    V_AO_IL_MAINTACTIVITYTYPES AS MAT
